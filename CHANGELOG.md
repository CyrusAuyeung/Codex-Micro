# 更新记录

## 2.1.0

- Codex 模式新增快捷键录入，支持左右修饰键、单独修饰键及组合键。
- 增强旋钮和摇杆：纵向／横向滚动、步长、方向反转、摇杆灵敏度与长拨连发。
- 新增 Codex 输入实时高亮和“仅测试”；测试时只显示动作，隐藏窗口后自动结束。
- 两种模式均支持多套配置、新建切换、导入导出和备份恢复；兼容旧配置，保护未保存草稿。
- 两种模式共用设备信息，显示电量、固件版本和充电状态；历史信息标注“上次读取”及时间。
- 支持软件内下载更新、进度、取消和重试；校验后安装并重启，保留配置与开机启动选择。
- 新增可选开机启动，静默进入托盘；修复托盘左键单击无法恢复窗口的问题。
- 模式切换不再重新加载页面，保留键位选择与读取状态；统一布局、按钮尺寸、下拉箭头和操作文案，清理冗余说明。
- 居中 m 图标；旋钮、摇杆和触摸点使用圆形高亮，未设置功能的预览统一显示横杠。

普通模式的“保存修改”保存本地配置，“写入键盘”通过配置热点预览并确认同步。旋钮转动、摇杆方向和实时高亮在 Codex 模式使用。

## 2.0.1

- 统一两种模式的键盘外壳、按键、旋钮和摇杆尺寸，切换模式时保持相同位置与比例。
- 修复 Windows 高缩放比例下窗口被放大而导致的模糊，启用逐显示器 DPI 渲染。
- 增加「m＋橙点」软件图标，用于桌面快捷方式、任务栏、托盘和安装包。

## 2.0.0

- 改为独立桌面窗口，关闭窗口后驻留托盘，再次启动恢复已有窗口。
- 两种模式的键盘与编辑器移到首屏，连接帮助、诊断和版本信息按需打开。
- 提供当前用户安装包，缺少 WebView2 时联网补装，覆盖安装保留配置。
- 增加新版提示和安装包下载入口。
- 补充社区行为准则和安全问题报告方式。

## 1.1.6

- 录入时松开部分按键，继续显示完整组合；按下新主键后再更新预览。
- 纯修饰键组合也保留完整预览，全部松开后可继续试按或确认。

## 1.1.5

- 支持单修饰键、多修饰键和无主键的组合。
- 支持保持修饰键、替换主键，以及多轮试按。
- 点击“使用此组合”后才完成录入，松开顺序不改变候选结果。

## 1.1.4

- 快捷键录入实时显示按键。
- 简化便携包中的使用说明。

## 1.1.3

- 增加 Windows 输入辅助程序，录入期间拦截组合键，避免触发截图等全局快捷键。
- 改善页面焦点、输入法和录入交互。
- 完善热点 IP 分配与网络冲突的诊断。

## 1.1.2

- 普通模式使用与实体一致的俯视布局：13 颗按键、底部三颗并排小键和旋钮按下。
- 收起不对应普通实体按键的额外配置位。

## 1.1.1

- 增加配置热点、IP 分配和设备响应的连接诊断。
- 增加临时 IP 修复入口，改善设备请求的网络接口选择。

## 1.1.0

- 新增普通键盘配置页，通过键盘 Wi-Fi 读取和保存键位。
- 支持配置导入导出、修改预览、写入前备份与写入后核对。
- 将保存到设备与 Codex 本机改键分为独立页面。

## 1.0.0

- 首个 Windows 版本。
- 支持 Codex 模式的按键识别与本机功能映射。
- 提供实体键盘布局、旋钮和摇杆动作配置以及系统托盘入口。
